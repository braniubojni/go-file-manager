# Claude / multi-agent entry

Canonical project memory is the **`AGENTS.md` tree** (Grok + shared). Do **not** duplicate long rules here.

1. Always read root **`AGENTS.md`** first.
2. Before editing a tree, read that module’s **`AGENTS.md`**:

| When working under… | Load                 |
| ------------------- | -------------------- |
| `frontend/`         | `frontend/AGENTS.md` |
| `internal/`         | `internal/AGENTS.md` |
| `build/`            | `build/AGENTS.md`    |
| `.github/`          | `.github/AGENTS.md`  |
| `e2e/`              | `e2e/AGENTS.md`      |

3. Human README: `README.md`. Cap: keep each AGENTS file ≤200 lines; update when conventions change.
